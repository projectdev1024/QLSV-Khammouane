@model IEnumerable<WebsiteMVC.Models.TinTuc>

@for (int i = 0; i < Model.Count(); i++)
{
    <div class="news-mt-Ex even @(i%3==2?"last":"")">
        <div class="news-3tin-title">
            <a href="@Url.Action("ChiTiet","TinTuc",new { Model.ElementAt(i).IDTinTuc })">
                <img src="@Model.ElementAt(i).Image" />
            </a>
        </div> <p>
            <a href="@Url.Action("ChiTiet","TinTuc",new { Model.ElementAt(i).IDTinTuc })"> @Model.ElementAt(i).TomTat ... <i class=""></i> </a>
        </p>
    </div>
}
